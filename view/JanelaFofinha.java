package view;

import javax.swing.*;
import java.awt.*;
/*
public class JanelaFofinha {
    public static void main(String[] args) {
        JFrame frame = new JFrame("Janela Fofinha");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(400, 300);
        frame.getContentPane().setBackground(new Color(255, 192, 203)); // Rosa claro

        // Layout do conteúdo da janela
        JPanel panel = new JPanel();
        panel.setLayout(new FlowLayout());

        // Botões em azul claro
        JButton button1 = new JButton("Botão 1");
        button1.setBackground(new Color(173, 216, 230)); // Azul claro
        JButton button2 = new JButton("Botão 2");
        button2.setBackground(new Color(173, 216, 230)); // Azul claro

        // Adicionando os botões ao painel
        panel.add(button1);
        panel.add(button2);

        // Adicionando o painel à janela
        frame.add(panel);

        frame.setVisible(true);
    }
}
*/

/* 
public class JanelaFofinha {
    public static void main(String[] args) {
        JFrame frame = new JFrame("Farmácia App");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(400, 300);
        frame.getContentPane().setBackground(Color.PINK);
        frame.setLayout(new FlowLayout());

        JLabel label = new JLabel("Pesquisar Produto:");
        JTextField textField = new JTextField(20);
        JButton button = new JButton("Pesquisar");

        frame.add(label);
        frame.add(textField);
        frame.add(button);

        frame.setVisible(true);
    }
}
*/